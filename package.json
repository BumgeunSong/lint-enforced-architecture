{
  "name": "lint-enforced-architecture",
  "version": "0.1.0",
  "private": true,
  "description": "Custom ESLint rules that enforce architecture, boundaries, and naming as a deterministic gate — a concrete example for the article.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "keywords": ["eslint", "eslint-plugin", "architecture", "lint", "ai"],
  "license": "MIT",
  "devDependencies": {
    "@typescript-eslint/parser": "^8.13.0",
    "eslint": "^9.14.0",
    "vitest": "^2.1.4"
  }
}
